# Marks Lombok-generated code with @lombok.Generated, which JaCoCo excludes from coverage.
# Without this, the equals/hashCode/toString of every @Value entity counts as untested lines --
# and writing tests for generated methods would be padding the number, not testing anything.
config.stopBubbling = true
lombok.addLombokGeneratedAnnotation = true
